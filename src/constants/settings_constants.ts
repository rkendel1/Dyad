export const MAX_CHAT_TURNS_IN_CONTEXT = 3;

// Chunking configuration constants
export const DEFAULT_CHUNK_SIZE_CHARS = 8000; // Default chunk size in characters
export const MIN_CHUNK_SIZE_CHARS = 2000; // Minimum chunk size
export const MAX_CHUNK_SIZE_CHARS = 15000; // Maximum chunk size
export const CHUNK_OVERLAP_CHARS = 200; // Overlap between chunks to maintain context
